# 논문 주제

동일한 DILAB RAG 시스템을 PostgreSQL + pgvector와 Oracle AI Database에서 각각 구현하고, 벡터 검색의 동등성, 관계형 메타데이터 결합 방식, 보안·감사·암호화 구성, 애플리케이션 이식 비용을 비교한다.

이 연구는 특정 데이터베이스의 우수성을 전제하지 않는다. DILAB의 실제 검색 계약과 운영 요구를 두 환경에서 재현하고, 차이가 발생하는 지점과 그 비용을 측정하는 것을 목적으로 한다.

> **기준 시점:** AS_OF 2026-07-10
>
> **대상 시스템:** 소비자·전문가 리뷰를 검색하여 근거가 포함된 제품 분석과 RAG 답변을 생성하는 DILAB
>
> **범위 원칙:** DILAB에서 구현했거나 비교 실험으로 검증할 수 있는 항목만 다룬다.

# 연구 목적

본 연구의 목적은 다음 세 질문에 답하는 것이다.

1. 동일한 데이터, 임베딩, 거리 함수, 필터 조건에서 두 데이터베이스가 동등한 검색 결과를 제공하는가.
2. DILAB의 벡터 검색과 관계형 메타데이터 조건을 각 환경에서 어떤 구조와 코드로 구현하는가.
3. 접근 제어, 감사, 저장 데이터 암호화, 애플리케이션 운영을 구성할 때 필요한 요소와 이식 비용은 어떻게 다른가.

비교 결과를 데이터베이스 엔진에 귀속하려면 코퍼스, 임베딩 모델·차원, 거리 함수, Top-K, 필터 조건을 동일하게 통제해야 한다. 현재 PostgreSQL 기준 구현은 외부에서 생성한 BGE-M3 1024차원 임베딩을 사용한다. 현재 Oracle 애플리케이션 구현은 데이터베이스 내부의 `DILAB_E5` 모델로 384차원 임베딩을 생성한다. 따라서 현재 두 애플리케이션의 종단 간 결과 차이는 데이터베이스뿐 아니라 임베딩 모델과 실행 구조의 차이도 포함한다.

데이터베이스 엔진만 비교하는 실험에서는 Phase 1 검증과 같이 동일한 BGE-M3 1024차원 벡터를 양쪽에 적재해야 한다. 현재 Oracle의 384차원 in-DB 임베딩 구성은 별도의 시스템 수준 비교로 다룬다.

현재 구현 상태는 다음과 같다.

| 구분 | PostgreSQL + pgvector 기준 구현 | Oracle AI Database 구현 |
|---|---|---|
| 저장·검색 | Supabase PostgreSQL, `vector(1024)`, HNSW cosine, `match_chunks` RPC | Oracle AI Database 26ai, 현재 `VECTOR(384, FLOAT32)`, HNSW cosine, `VECTOR_EMBEDDING`·`VECTOR_DISTANCE` |
| 임베딩 | BGE-M3 1024차원, 데이터베이스 외부 생성 | `DILAB_E5` 384차원, 데이터베이스 내부 생성 |
| 검색 조건 | 도메인·제품·출처 필터, 전문가 우선 | 도메인·제품·출처 필터, 소스별 Top-K, 전문가 우선 |
| 데이터 접근 | Supabase 클라이언트, PostgREST/RPC, RLS 정책 | `node-oracledb` 직접 SQL, Python Oracle 어댑터 |
| 실행 환경 | 운영 원본의 Cloudflare·Supabase 구성 | 분리 복제본의 로컬 Node 실행과 Oracle wallet 연결 |
| 권한 범위 | RLS 정책이 정의되어 있으나 현재 공개 단일 테넌트가 우선 범위 | 공개 단일 테넌트가 우선 범위이며 VPD·Auth 대체는 미구현 |

# 비교 항목

## Vector Search

### 현재 내용

두 구현 모두 코사인 거리와 HNSW 기반 ANN(Approximate Nearest Neighbor) 인덱스를 사용할 수 있다. 따라서 Oracle이 HNSW를 사용한다는 사실만으로 더 빠르다고 결론 내릴 수 없다.

PostgreSQL 구현은 `embedding <=> query_vector`를 사용한다. Oracle 구현은 `VECTOR_DISTANCE(embedding, query_vector, COSINE)`을 사용한다. Phase 1에서는 동일한 BGE-M3 1024차원 벡터를 양쪽에 적재하여 Top-K 결과를 대조했다. 표본 3개 중 2개는 순서가 완전히 일치했고, 1개는 ANN 결과의 후순위에서 차이가 있었다. 이 결과는 제한된 표본의 동등성 점검이며 일반적인 성능 우위를 뜻하지 않는다.

현재 Oracle 애플리케이션은 `DILAB_E5` 384차원 in-DB 임베딩으로 전환되었다. 이 결과를 BGE-M3 1024차원 PostgreSQL 결과와 직접 비교해 데이터베이스 차이로 해석해서는 안 된다.

### 비교 이유

RAG 답변의 근거 청크는 검색 순위에 의해 결정된다. 두 구현의 검색 결과가 동등한지 확인해야 이후의 답변 품질과 출처 정합성을 비교할 수 있다.

### DILAB에서 의미

DILAB은 전문가 리뷰와 일반 리뷰에서 질문에 가까운 청크를 검색하고, 해당 청크만 LLM의 근거로 제공한다. Vector Search 비교의 핵심은 단순 처리 속도보다 동일 조건에서 관련 청크를 안정적으로 회수하는지 여부다.

### 비교 가능한 지표

- 정확 검색을 기준으로 한 Recall@K
- 두 구현의 Top-K overlap과 순위 상관
- p50·p95 질의 지연시간과 처리량
- 인덱스 생성 시간, 인덱스 크기, 메모리 사용량
- 코퍼스 크기와 필터 선택도에 따른 성능 변화
- 동일 근거 집합을 사용한 RAG 인용 정합률

## Metadata Query

### 현재 내용

PostgreSQL과 Oracle 모두 관계형 SQL로 메타데이터를 조회하고 벡터 조건과 결합할 수 있다. “Oracle은 가능하고 pgvector는 불가능하다”는 구분은 성립하지 않는다.

DILAB의 PostgreSQL `match_chunks`는 도메인, 제품, 출처 유형과 전문가 우선 조건을 처리한다. 현재 Oracle 검색 SQL도 도메인, 제품, 출처 유형을 필터링하고 전문가·일반 리뷰를 구분한다. 감성, 카테고리, Journey 결과는 별도 관계형 테이블에 저장되어 있지만, 현재 RAG 검색 SQL에는 이 테이블들이 결합되어 있지 않다.

Oracle의 비교 지점은 메타데이터 질의의 가능 여부가 아니라 벡터·관계형·JSON 데이터를 동일 SQL 및 데이터베이스 관리 체계에서 다루는 방식이다. PostgreSQL에서도 동일한 구조를 SQL, 조인, JSONB, 함수로 구현할 수 있다.

### 비교 이유

DILAB의 검색 범위는 도메인, 제품, 출처 유형에 따라 달라진다. 메타데이터 조건을 검색 전후 어느 단계에서 적용하는지에 따라 결과의 정확성, 질의 비용, 코드 복잡도가 달라질 수 있다.

### DILAB에서 의미

현재 구현에서 Metadata Query는 다음 조건을 뜻한다.

- `domain_id`에 의한 도메인 범위 제한
- `product_id`에 의한 제품 범위 제한
- `source_type`에 의한 전문가·일반 리뷰 구분
- 전문가 리뷰 우선 또는 소스별 Top-K 구성

감성·카테고리·Journey 조건을 포함한 검색은 구현 후 별도의 비교 대상으로 추가해야 하며, 현재 기능으로 간주하지 않는다.

### 비교 가능한 지표

- 필터 선택도별 p50·p95 지연시간
- 필터 적용 전후 Recall@K와 반환 건수
- SQL 문장 수, 애플리케이션 왕복 횟수, 후처리 코드량
- 실행계획의 인덱스 사용 여부와 실제 처리 행 수
- 동일 조건에서 반환된 청크와 순서의 일치율

## Hybrid Query

### 현재 내용

이 연구에서 Hybrid Query는 키워드 검색과 벡터 검색의 결합만을 뜻하지 않는다. DILAB의 범위에서는 다음 요소를 하나의 검색·분석 흐름에서 결합하는 질의로 정의한다.

> **Vector Search + Metadata Filter + 관계형 SQL + 필요 시 Aggregation**

현재 구현된 범위는 벡터 유사도, 도메인·제품·출처 필터, 소스별 순위 계산, 전문가 우선 정렬이다. Oracle의 `retrieveHybrid`는 질문 임베딩을 한 번 생성하고, `ROW_NUMBER()`로 출처 유형별 Top-K를 계산한 뒤 결과를 합친다. PostgreSQL의 `match_chunks`도 벡터 거리와 메타데이터 조건을 SQL 함수에서 처리한다.

감성·카테고리·Journey 조인과 집계까지 포함한 단일 검색 SQL은 현재 구현 근거가 없다. 이 항목은 구현 후 평가해야 한다.

### 비교 이유

벡터 검색만 분리해서 측정하면 DILAB이 실제로 수행하는 필터링과 우선순위 계산 비용을 반영하지 못한다. Hybrid Query는 실제 RAG 검색 계약을 기준으로 두 구현을 비교하기 위한 단위다.

### DILAB에서 의미

현재 DILAB의 Hybrid Query는 “지정한 도메인과 제품에서 전문가 리뷰 N개와 일반 리뷰 M개를 각각 의미 유사도 순으로 검색하고, 전문가 근거를 먼저 제공하는 질의”다.

### 비교 가능한 지표

- 전체 질의 지연시간과 데이터베이스 실행시간
- 데이터베이스 호출 횟수와 전송 데이터량
- SQL·함수·애플리케이션 후처리 코드의 규모
- 소스별 Top-K 충족률과 최종 순위의 재현성
- 조건 추가에 따른 실행계획과 지연시간 변화
- 집계를 추가할 경우 정확성, 임시 데이터 사용량, 처리시간

## Access Control

### 현재 내용

PostgreSQL은 Row-Level Security(RLS)를 제공하며, DILAB의 Supabase 스키마에는 공개 도메인·소유자 접근과 사용자별 행 접근을 위한 정책이 정의되어 있다. Supabase Auth의 사용자 식별값을 RLS 조건에 연결하는 방식이다.

Oracle은 Role, grant, 애플리케이션 컨텍스트와 Virtual Private Database(VPD)를 조합하여 행 단위 정책을 구성할 수 있다. 이는 PostgreSQL에 없는 접근 제어 기능이라는 뜻이 아니라 정책 정의, 사용자 신원 전달, 운영 도구의 구성이 다르다는 뜻이다.

현재 연구 범위는 로그인 없는 공개 단일 테넌트다. Oracle VPD와 Auth 대체는 구현되지 않았다. 따라서 현재 단계에서 RLS와 VPD의 효과나 운영 우위를 실험 결과처럼 서술하지 않는다. 멀티테넌트 단계에서는 애플리케이션 또는 외부 IdP가 인증한 사용자 ID를 Oracle 세션 컨텍스트에 전달하는 설계가 추가로 필요하다.

### 비교 이유

기업별 비공개 리뷰를 수용하면 동일 테이블의 데이터를 테넌트와 사용자별로 격리해야 한다. 접근 제어는 벡터 검색 결과에도 동일하게 적용되어야 한다.

### DILAB에서 의미

비교 대상은 공개 도메인 조회, 소유자 전용 도메인 조회, 본인의 질문·리포트 조회, 분석 워커의 제한된 쓰기 권한이다. 현재 단일 테넌트에서는 이 시나리오를 사용하지 않으므로 별도 실험 환경에서 검증해야 한다.

### 비교 가능한 지표

- 동일 권한 시나리오를 구현하는 정책·함수·애플리케이션 코드량
- 인증 정보가 데이터베이스 정책에 전달되는 단계 수
- 허용·차단 테스트의 통과율과 정책 누락 건수
- 정책 적용 전후 질의 지연시간
- 정책 변경과 배포 절차, 필요한 관리자 권한
- 커넥션 풀 재사용 시 사용자 컨텍스트 격리 여부

## Audit

### 현재 내용

PostgreSQL에도 표준 로그 설정과 pgAudit 같은 확장을 이용한 감사 구성이 가능하다. Oracle만 감사 기능을 제공하는 것은 아니다.

Oracle Unified Auditing은 감사 정책과 여러 감사 레코드를 통합된 감사 추적에서 관리하는 방식을 제공한다. 비교의 초점은 기능 유무가 아니라 정책 표현, 기록 범위, 로그 저장·조회·보존, 역할 분리의 차이다.

현재 DILAB 코드와 마이그레이션에는 두 환경의 감사 정책을 동일 요구사항으로 구성하고 검증한 결과가 없다. Sentry 관측은 애플리케이션 오류 추적이며 데이터베이스 감사와 동일하지 않다. 따라서 Audit은 향후 구현·측정 항목이다.

### 비교 이유

DILAB이 비공개 기업 데이터를 처리하면 누가 어떤 데이터에 접근했는지 추적할 수 있어야 한다. 다만 모든 조회를 기록하면 저장량과 성능 비용이 증가하므로 동일한 감사 요구사항으로 비교해야 한다.

### DILAB에서 의미

우선 비교할 이벤트는 로그인 성공·실패, 비공개 청크 조회, 질문·리포트 조회, 데이터 변경, 권한·정책 변경이다. LLM 호출 로그와 데이터베이스 감사 로그는 목적과 보존 정책을 분리한다.

### 비교 가능한 지표

- 요구 이벤트별 감사 기록 누락률
- 감사 정책 수와 설정 단계
- 로그 필드의 일관성, 검색 시간, 보존·삭제 절차
- 관리자와 감사 조회자의 역할 분리 방식
- 감사 활성화 전후 저장량과 질의 처리 오버헤드
- 애플리케이션 사용자와 데이터베이스 세션의 추적 가능성

## TDE

### 현재 내용

저장 데이터 암호화는 다음 계층을 구분해야 한다.

- **Disk Encryption:** 물리 디스크 또는 볼륨 전체를 암호화한다.
- **OS Encryption:** 운영체제나 파일시스템 계층에서 파일을 암호화한다.
- **Cloud Encryption:** 클라우드 사업자가 스토리지 서비스와 키 관리 체계에서 암호화를 제공한다.
- **Database Encryption(TDE):** 데이터베이스가 데이터 파일 또는 지정한 데이터 영역의 암·복호화와 키 사용을 관리한다.

PostgreSQL이 암호화를 지원하지 않는 것은 아니다. 배포 환경의 디스크·OS·클라우드 암호화, TLS, 애플리케이션 암호화, `pgcrypto`를 이용한 컬럼 수준 암호화 등을 조합할 수 있다. 다만 이러한 방식은 각각 적용 계층과 키 관리 주체가 다르며 Oracle TDE와 동일한 기능으로 간주할 수 없다.

Oracle TDE는 데이터베이스 계층에서 저장 데이터 암호화와 키 저장소를 관리한다. 이 연구에서의 비교 지점은 “암호화 가능 여부”가 아니라 동일한 저장 데이터 보호 범위를 어떤 계층과 절차로 구성하는가이다.

현재 저장소에는 PostgreSQL 기준 암호화 구성이나 Oracle TDE 활성화 상태를 검증하는 코드·실험 결과가 없다. 실제 활성화 여부와 서비스별 기본 설정은 실험 환경에서 확인해야 한다.

### 비교 이유

리뷰 원문, 기업 전용 데이터, 생성 리포트의 저장 위치가 여러 계층에 걸칠 수 있다. 암호화 범위와 키 관리 책임을 분리해 비교해야 보호 공백과 운영 비용을 판단할 수 있다.

### DILAB에서 의미

암호화 대상은 데이터 파일, 백업, 임시 영역, 감사 로그, wallet·키 저장소, 네트워크 전송 구간이다. 벡터 데이터도 원문에서 파생된 데이터이므로 동일한 보호 범위에 포함할지 명시해야 한다.

### 비교 가능한 지표

- 데이터 파일·백업·임시 영역별 암호화 적용 범위
- 초기 설정, 키 생성·보관·회전·복구 단계 수
- 애플리케이션 코드 변경량과 운영자 권한
- 암호화 전후 읽기·쓰기·백업 성능
- 키 분실·복구와 재해 복구 절차의 검증 결과
- 클라우드 제공 기능과 데이터베이스 기능의 책임 경계

## 운영 편의성

### 현재 내용

운영 편의성은 “Oracle이 더 편하다” 또는 “Supabase가 더 단순하다”와 같은 주관적 문장으로 평가하지 않는다. 동일한 DILAB 기능을 실행·배포·복구하는 데 필요한 구성 요소와 변경량으로 비교한다.

PostgreSQL 기준 구현은 Supabase의 PostgreSQL, Auth, RLS, PostgREST/RPC, Storage와 Cloudflare 실행 환경을 사용했다. 이 조합은 관리형 서비스의 통합 기능을 활용하지만 여러 서비스의 설정과 배포 계약에 의존한다.

현재 Oracle 구현은 `node-oracledb` 직접 SQL, Python Oracle 어댑터, wallet 연결, in-DB 임베딩을 사용한다. 데이터 접근과 임베딩을 Oracle에 모았지만 Supabase 클라이언트·RPC를 직접 SQL과 어댑터로 교체했다. 또한 `node-oracledb`는 기존 Cloudflare edge 실행 환경과 호환되지 않아 이 복제본은 로컬 Node 실행을 기준으로 한다. 인증 대체와 VPD는 아직 운영 요소에 포함되지 않았다.

따라서 통합성은 자동으로 편의성을 의미하지 않는다. 외부 임베딩 서비스 의존성 감소, 직접 SQL 증가, 드라이버·wallet 관리, 배포 대상 변경, Auth 대체 필요를 함께 측정해야 한다.

### 비교 이유

벡터 타입과 거리 함수의 변환보다 데이터 접근 계층, 인증, API, JSON·배열 타입, 배포 런타임의 변경이 전체 이식 비용을 좌우할 수 있다.

### DILAB에서 의미

실제 이식 범위에는 `supabase-js`·`supabase-py` 호출 교체, `match_chunks` 재작성, JSON·배열 매핑, Oracle 연결 풀과 wallet 설정, 분석 파이프라인의 쓰기 경로, 로컬·클라우드 실행 환경 조정이 포함된다.

### 비교 가능한 지표

- 변경 파일 수, 변경 LOC, 신규 어댑터·SQL 수
- 스키마 변환과 데이터 이관에 걸린 시간 및 실패 건수
- 환경 변수, 비밀정보, 드라이버, wallet 등 설정 항목 수
- 배포·재기동·백업·복구 절차의 단계 수와 소요 시간
- 데이터베이스·인증·API·임베딩·관측 서비스의 구성 요소 수
- 외부 서비스 장애가 RAG 기능에 미치는 영향과 단일 장애 지점
- 로컬 개발, 테스트, 운영 환경 사이의 구성 차이
- 동일 회귀 테스트의 통과율과 장애 진단 시간

# 현재까지의 결론

1. **Vector Search만으로 우열을 결론 내릴 근거는 없다.** 두 환경 모두 HNSW 기반 ANN과 코사인 검색을 사용할 수 있다. 동일 BGE-M3 벡터를 사용한 제한된 Phase 1 검증은 높은 Top-K 동등성을 보였지만 성능 우위를 입증하지 않는다.
2. **Metadata Query와 Hybrid Query는 두 환경 모두 구현할 수 있다.** 현재 DILAB에서 검증된 범위는 도메인·제품·출처 필터와 전문가 우선 검색이다. 감성·카테고리·Journey·Aggregation을 한 검색 질의에 결합한 것으로 확대 해석하지 않는다.
3. **Oracle 전환의 주요 비용은 벡터 연산보다 데이터 접근과 부가기능의 교체에서 발생했다.** Supabase 클라이언트·RPC는 직접 SQL과 Oracle 어댑터로 바뀌었고, wallet과 실행 환경 관리가 추가되었다. 반면 in-DB 임베딩은 외부 임베딩 실행 요소를 줄였다.
4. **Access Control, Audit, TDE는 아직 공정한 실험 결과가 없다.** PostgreSQL RLS와 Oracle VPD, PostgreSQL 감사 구성과 Oracle Unified Auditing, 인프라 암호화와 Oracle TDE는 모두 동일 요구사항으로 구현한 뒤 비교해야 한다.
5. **현재 연구의 적절한 결론은 우수성 판단이 아니라 적용 조건의 구분이다.** 단순 벡터 검색의 동등성, 관계형 조건 결합 방식, 보안 기능의 관리 모델, 마이그레이션 코드량과 운영 요소를 측정하여 각 구성이 DILAB에 주는 비용과 제약을 설명하는 것이 논문의 범위다.
