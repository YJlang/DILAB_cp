"""발표자 노트 (한국어) — 슬라이드는 영문, 입은 한국어.

배정된 20분에는 질의응답이 포함된다. 발표를 15분에 끊고 5분을 남긴다는
전제로 시간을 배분했다. 각 노트 첫 줄의 `[구간 · 소요]` 가 그 기준이다.

대본을 그대로 읽지 말 것. 슬라이드의 한국어 리드 한 줄을 말하고, 아래 요점을
자기 말로 풀면 시간이 맞는다. ★ 표시는 이 발표에서 반드시 전달해야 하는 것이다.
"""

COVER = """
[0:00–0:20 · 20초]
안녕하십니까. 성결대학교 윤준하입니다.
'벡터 데이터베이스를 옮길 때 저장소 교체와 임베딩 위치의 효과를 분리하는 방법'으로 발표하겠습니다.
자료는 영문이지만 발표는 한국어로 진행하겠습니다.
"""

NOTES = [
    # 2 ─────────────────────────────────────────────────────────────────
    """
[0:20–1:40 · 80초]
발표 전체를 한 장으로 먼저 보여드립니다. 결론을 알고 들으시면 뒤가 쉽습니다.

· 문제 — 벡터 레이어를 옮기면 두 가지가 동시에 바뀝니다. 그래서 전후를 한 번 비교해서는
  어느 쪽이 원인인지 알 수 없습니다.
· 제안 — 중간 조건을 하나 끼워 넣어 비교 한 번을 두 번으로 쪼갭니다.
· 결과 — 저장소를 바꾼 데서는 변화가 검출되지 않았고, 변화는 사실상 전부 임베딩 위치에서
  나왔습니다.

지금부터 이 셋을 순서대로 말씀드리겠습니다.
""",
    # 3 ─────────────────────────────────────────────────────────────────
    """
[1:40–2:30 · 50초]
빠르게 배경만 짚겠습니다.

· RAG는 임베딩 모델, 저장소, 언어 모델 세 부분으로 이뤄집니다.
· 이 중 배포 후 가장 자주 바뀌는 것이 벡터 레이어입니다.
· 대표적인 경로가 '범용 DB + 벡터 확장'에서 '벡터 타입과 추론 런타임을 내장한 융합형 DB'로
  가는 것입니다. 데이터베이스가 임베딩까지 직접 계산하는 구성입니다.

이런 이전의 효과는 보통 전후를 한 번 비교해서 평가합니다. 여기서 문제가 생깁니다.
""",
    # 4 ─────────────────────────────────────────────────────────────────
    """
[2:30–4:00 · 90초]  ★ 이 발표의 첫 번째 승부처

두 요인을 분명히 구분해서 말합니다.
· 저장소 — 벡터를 담는 엔진과 인덱싱 방식. 근사 검색이냐 정확 검색이냐.
· 위치 — 임베딩을 DB 밖에서 계산하느냐, 안에서 계산하느냐.

★ 중요한 건 '왜 둘이 같이 바뀌는가' 입니다. 우연이 아닙니다.
  거버넌스 때문에 옮기는 이전은 임베딩도 안으로 넣는 구성을 원합니다.
  그런데 모델을 품는 DB는 모델 크기에 제한이 있습니다.
  그래서 위치를 옮기면 모델 용량까지 딸려서 바뀝니다.

결론 — 전후 차이만으로는 어느 요인이 변화를 일으켰는지 지목할 수 없습니다.
""",
    # 5 ─────────────────────────────────────────────────────────────────
    """
[4:00–4:50 · 50초]
이게 왜 실무에서 위험한지 짧게 말합니다.

· 품질이 떨어져도 저장소 탓인지 모델 탓인지 모릅니다.
· 변화가 없어도, 정말 영향이 없었던 건지 두 요인이 상쇄한 건지 모릅니다.
· 그래서 처방이 어긋납니다. 저장소를 탓해서 되돌려도 품질은 돌아오지 않습니다.

(뒤에 실제로 그럴 뻔했던 수치가 나온다고 예고해두면 결과 슬라이드가 살아납니다)
""",
    # 6 ─────────────────────────────────────────────────────────────────
    """
[4:50–6:50 · 120초]  ★★ 가장 중요한 슬라이드. 여기서 서두르지 말 것

그림을 왼쪽부터 짚어가며 말합니다.
· A — 지금 운영 중인 시스템입니다. PostgreSQL에 pgvector, 임베딩은 밖에서 계산합니다.
· C — 이전 후 시스템입니다. 저장소도 바뀌고 임베딩도 DB 안으로 들어갔습니다.
  A와 C를 바로 비교하면 두 가지가 한꺼번에 바뀝니다.
· B — 그래서 가운데에 조건을 하나 만듭니다. 저장소는 C와 같지만,
  ★ A가 이미 만들어 둔 벡터를 그대로 실어 놓습니다.

그러면 A→B는 저장소만 다르고, B→C는 위치만 다릅니다. 한 번에 한 요인씩 비교됩니다.

★ 비용을 꼭 언급하세요. 재임베딩이 필요 없습니다. 운영 테이블은 그대로 두고
  벡터 컬럼 하나만 더 만들어 채우면 되므로, 준비 비용은 벌크 로드 한 번이고
  돌아가는 서비스는 건드리지 않습니다.
""",
    # 7 ─────────────────────────────────────────────────────────────────
    """
[6:50–7:40 · 50초]
구조 차이는 두 가지만 짚고 넘어갑니다.

· 네트워크 경계가 2개에서 1개로 줄어듭니다.
· 외부 모델 호출이 사라집니다. 질의문이 DB 경계를 벗어나지 않습니다.

★ 한 가지 반드시 덧붙이세요 — 검색 규칙(벡터 유사도 + 메타데이터 필터 + 출처 우선순위)과
  그 파라미터는 세 조건에서 똑같이 유지했습니다. 안 그러면 비교가 성립하지 않습니다.
""",
    # 8 ─────────────────────────────────────────────────────────────────
    """
[7:40–8:40 · 60초]
표는 읽지 말고 '무엇이 한 칸씩만 다른가'를 보여주세요.

· A와 B는 저장소 행만 다릅니다.
· B와 C는 임베딩 모델과 위치 행이 다릅니다.
· 코퍼스는 실운영 리뷰 1,347개 청크, 질의는 43개로 고정했습니다.
· 지표는 셋입니다 — 검색 결과가 얼마나 겹치는지, 근거가 얼마나 관련 있는지, 얼마나 걸리는지.

관련성은 LLM이 0~2점으로 채점했고, 신뢰구간은 부트스트랩 1만 회로 구했습니다.
""",
    # 9 ─────────────────────────────────────────────────────────────────
    """
[8:40–10:00 · 80초]
저장소만 바꾼 경우입니다.

· top-10 자카드 0.971. 43개 질의 중 37개는 상위 10개가 완전히 같았습니다.
· 관련성 점수 차이는 −0.042, p는 0.523으로 유의하지 않고 효과크기도 무시할 수준입니다.
· 기준 시스템이 근사 인덱스 때문에 놓친 결과는 1.6% 정도입니다.

즉, 저장소를 바꾼 것만으로는 검색 품질의 변화가 검출되지 않았습니다.
(다음 장과 대비되므로 숫자를 또렷하게 말하고 넘어가세요)
""",
    # 10 ────────────────────────────────────────────────────────────────
    """
[10:00–11:50 · 110초]  ★ 대비가 살아나는 지점

· 같은 저장소, 같은 검색 방식인데 자카드가 0.175로 떨어집니다.
  앞 장의 0.971과 비교해서 말씀하세요. 같은 질의에 대해 대체로 다른 근거가 나옵니다.
· 관련성은 1.270에서 1.014로, p는 0.003, 효과크기는 0.473으로 중간 크기입니다.

★ 이 발표의 결론 문장 —
  A→B에서 저장소 효과가 검출되지 않았으므로, 전체 품질 변화는 사실상 전부
  '위치 요인과 거기 딸려 온 모델 크기 제한' 때문입니다.

★ 그리고 실무적 함의 —
  만약 전후 비교만 했다면 관련성 20% 하락을 저장소 탓으로 돌렸을 겁니다.
  그 진단을 따라 저장소를 되돌려도 품질은 회복되지 않습니다.
""",
    # 11 ────────────────────────────────────────────────────────────────
    """
[11:50–12:50 · 60초]
여기는 솔직하게 말하는 대목입니다. 심사에서도 이 부분을 좋게 봤습니다.

· '차이가 없다'가 우리 주장이라면, 유의하지 않은 p값에 기대면 안 됩니다.
· 그래서 ±0.15점 범위로 등가성 검정을 따로 돌렸습니다. 결과는 p = 0.052로 미달입니다.
  신뢰구간 하한 −0.172가 범위를 벗어났습니다.
· 그래서 '동등함이 입증됐다'가 아니라 '효과가 검출되지 않았고 크기는 작은 범위로
  제한된다'고 보고합니다.
· 다만 구간이 벌어진 방향은 목표 저장소가 오히려 높은 쪽이라,
  저장소 교체가 품질을 떨어뜨렸다고 볼 근거는 없습니다.
""",
    # 12 ────────────────────────────────────────────────────────────────
    """
[12:50–14:20 · 90초]
· 방법 — 브리지 조건 하나면 전후 비교가 단일 요인 비교 두 개로 쪼개집니다.
· 관측 — 저장소 효과는 검출되지 않았고 변화는 전부 위치에서 왔습니다.

★ 한 문장으로 남길 메시지 —
  위치를 옮기는 것은 성능의 교환이 아니라 거버넌스의 교환입니다.
  질의와 원문을 DB 경계 안에 두는 대가로, 품을 수 있는 모델의 크기와
  그 DB 등급의 추론 성능을 받아들이는 겁니다.

실무 기준 두 가지로 마무리합니다.
· 저장 데이터의 거버넌스만 목적이면 저장소만 바꿔도 충분합니다.
· 질의 경로까지 경계 안에 둬야 한다면, 품을 수 있는 최대 모델이 그 도메인에
  충분한지 먼저 확인해야 합니다. 브리지 조건이 그 확인을 이전 전에 가능하게 합니다.
""",
    # 13 ────────────────────────────────────────────────────────────────
    """
[14:20–15:00 · 40초] 그리고 질의응답 5분

한계는 빠르게 넘어갑니다.
· 단일 한국어 코퍼스, 사람 평가자 미참여, 엔트리급 인스턴스.
· 설계상 위치와 모델 크기가 묶여 있습니다. 이걸 떼려면 작은 모델을 DB 밖에서 돌리는
  네 번째 조건이 필요하고, 향후 과제로 남겨두었습니다.

감사합니다. — 여기서 질문을 받습니다.

── 예상 질문 대비 ──────────────────────────────
Q. 특정 제품이 더 낫다는 주장인가요?
A. 아닙니다. 제품의 우열을 가리는 연구가 아니라, 이전의 효과를 요인별로 나누는 실험 설계를
   제안한 것입니다. 융합형 DB로 가는 이전 일반에 적용됩니다.

Q. 지연시간이 8배면 실용성이 없는 것 아닌가요?
A. 그 수치는 무료 등급 인스턴스에서 추론을 돌린 결과이지 in-database 임베딩 자체의 성질이
   아닙니다. 상위 하드웨어로 일반화할 수 없다고 논문에도 명시했습니다.

Q. B와 C는 모델이 다른데 공정한 비교인가요?
A. 그 결합이 바로 이 연구가 드러내려는 지점입니다. DB가 품을 수 있는 모델 크기에 제한이
   있어서, 위치를 안으로 옮기면 모델이 따라 바뀝니다. 의도적으로 결합을 남겨 두었고,
   분리하려면 네 번째 조건이 필요하다고 밝혔습니다.

Q. 질의 43개는 너무 적지 않나요?
A. 한계로 인정합니다. 다만 43개 중 13개는 실제 운영 로그에서 뽑았고, 합성 질의와 실제 질의를
   나눠 검증했을 때 '원인 귀속의 근거'가 되는 검색 일치도에서는 두 집단 간 유의한 차이가
   없었습니다(p = 0.441, 0.570).

Q. 왜 하필 자카드인가요?
A. 두 조건이 같은 질의에 대해 같은 근거를 가져오는지를 집합 수준에서 보기 위해서입니다.
   순위 유사도(스피어만)와 정확 검색 대비 재현율도 함께 측정했습니다.
"""
]
